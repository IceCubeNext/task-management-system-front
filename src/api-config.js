
const auth_path = 'http://localhost' + ':' + 8181;
const path = 'http://localhost' + ':' + 8080;
export const host = {
    auth_path,
    path,
    tasks: path + '/tasks',
    comments: path + '/comments',
}

